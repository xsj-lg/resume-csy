# Codex Kickoff Prompt（可直接复制）

请以文档先行方式推进本项目，并严格遵守以下流程：

1. 先读取 `docs/docs-index.md` 与 `docs/00-governance/*`。
2. 若是新需求，先在 `docs/01-requirements/req-00-draft.md` 补录或确认条目。
3. 进入执行前，将需求平移到 `docs/02-roadmap/roadmap-00-todolist.md` 并保留同一 `requirement_id`。
4. 研发过程中仅在 `roadmap-00` 维护执行状态。
5. 交付时输出：变更文件、需求编号、状态变化、风险与未完成项。
6. 若涉及发布，必须完成：
   - `roadmap-00` 待处理状态清零
   - `DONE` 任务归档到 `roadmap-01`
   - 回填 `req-01` 与 `req-02`

如果发现规则冲突，统一以 `docs/00-governance/` 为准。

