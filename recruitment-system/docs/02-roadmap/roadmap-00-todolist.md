# TodoList

## 1. 文档信息

- 当前版本: `0.1.15`
- 目标版本: `0.1.15`
- 作用: 记录目标版本执行中的任务清单与状态，作为发布前唯一执行事实源。

### 最近更新（最新 5 条）

| date | version | target_version | summary |
| --- | --- | --- | --- |
| `2026-03-26` | `0.1.15` | `0.1.15` | 1. `REQ-v0.1.15-005` 从 `req-00` 平移至本清单并进入执行。<br>2. 已完成协作治理补充：执行前强制重读 `docs/00-governance/`，且代码或文件变更必须同步更新对应 `project-docs` 描述。<br>3. 文档核对完成，`REQ-v0.1.15-005` 状态更新为 `DONE`，验证结果置为 `PASS`。<br>4. 用户确认已发布，`REQ-v0.1.15-005` 已归档到 `roadmap-01` 并完成 `req-01/req-02` 回填。 |
| `2026-03-25` | `0.1.15` | `0.1.15` | 1. `REQ-v0.1.15-004` 从 `req-00` 平移至本清单并进入 `IN_PROGRESS`。<br>2. 已启动上传评分表分段结构解析修复，补全维度下评分项与续行标准归属。<br>3. 功能验证完成，`REQ-v0.1.15-004` 状态更新为 `DONE`，验证结果置为 `PASS`。 |
| `2026-03-25` | `0.1.15` | `0.1.15` | 1. `REQ-v0.1.15-003` 从 `req-00` 平移至本清单并进入 `IN_PROGRESS`。<br>2. 已启动自动评分总分按评分项累加校验与前端评分项明细完整展示开发。 |
| `2026-03-25` | `0.1.15` | `0.1.15` | 1. `REQ-v0.1.15-002` 从 `req-00` 平移至本清单并进入 `IN_PROGRESS`。<br>2. 已启动工作台选中简历记录的前端持久化与返回恢复能力开发。 |
| `2026-03-24` | `0.1.15` | `0.1.15` | 1. `REQ-v0.1.15-001` 状态更新为 `IN_PROGRESS`，启动操作记录页面前端开发。<br>2. 已新增统一日志记录表、查询/详情/导出接口，并在登录、用户权限、岗位配置与候选人关键链路接入操作留痕。 |

## 2. 字段说明

- `target_version`: 目标版本。
- `requirement_id`: 需求编号（`REQ-v<version>-NNN`）。
- `task_status`: `PENDING` / `IN_PROGRESS` / `BLOCKED` / `CANCELLED` / `DONE`。
- `item`: 执行任务标题。
- `draft_ref`: 对应 `req-00-draft.md` 来源。
- `frontend_scope`: 前端改动拆分（是否改动 + 功能点清单；不涉及填 `N/A`）。
- `backend_scope`: 后端改动拆分（是否改动 + 功能点清单；不涉及填 `N/A`）。
- `verification_plan`: 验证计划（至少列出接口、页面、回归中的实际验证项）。
- `verification_result`: 验证结果（`TODO` / `PASS` / `FAIL` / `N/A`）。
- `done_definition`: 完成判定标准。

## 3. Current TODO List

### 快速清单

- `REQ-v0.1.15-001` (`IN_PROGRESS`): 新增操作记录页面。
- `REQ-v0.1.15-002` (`IN_PROGRESS`): 工作台记住上次查看简历。
- `REQ-v0.1.15-003` (`IN_PROGRESS`): 自动评分总分校验与评分项明细展示。
- `REQ-v0.1.15-004` (`DONE`): 上传评分表解析补全维度下评分项。
- `REQ-v0.1.15-005` (`DONE`): 治理补充执行前通读与 project-docs 同步规则。

### 任务详情

| target_version | requirement_id | task_status | item | draft_ref | frontend_scope | backend_scope | verification_plan | verification_result | done_definition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `0.1.15` | `REQ-v0.1.15-001` | `IN_PROGRESS` | 新增操作记录页面 | `req-00 4.2` | `是：新增操作记录页面入口、日志列表查询、多条件筛选、日志详情查看、关键字段高亮比对、导出能力、只读展示与空态/结果态反馈。` | `是：新增统一日志记录表；补充日志写入能力；在用户与权限、岗位配置、评分表管理、候选人档案维护、阶段流转、自动评分触发等关键链路接入操作留痕。` | `1) 操作记录页面支持按时间范围、操作人、模块、类型、业务对象、结果、来源等条件筛选。2) 日志详情可查看完整基础字段、备注与扩展上下文。3) 当存在同对象历史记录时，关键字段可高亮比对。4) 支持导出当前筛选结果与单条详情。5) 新增日志记录表字段满足 log_id、业务对象、操作人、时间、结果、客户端 IP、请求来源、备注等要求。6) 回归现有岗位级关键操作日志展示不受影响。` | `TODO` | `系统级操作记录页面的查询、筛选、详情、比对、导出能力可用，统一日志记录表与关键链路留痕落地，验证计划全部 PASS 后可置 DONE。` |
| `0.1.15` | `REQ-v0.1.15-002` | `IN_PROGRESS` | 工作台记住上次查看简历 | `req-00 4.2` | `是：保存工作台当前选中的候选人/简历记录；工作台重新进入时自动恢复选中态、详情区与简历预览；候选人列表自动滚动到已恢复记录附近；候选人失效时回退到首条可用记录或空态。` | `N/A` | `1) 在工作台点击某个候选人后切换到其他页面，再返回工作台时，自动恢复到该候选人。2) 恢复后列表可定位到该候选人附近。3) 当上次候选人已删除或当前不可见时，工作台可安全回退，不报错。4) 回归候选人列表筛选、排序、星标与详情加载行为不受影响。` | `TODO` | `工作台可记住并恢复上次查看的简历记录，且候选人不存在或不可见时可安全降级，验证计划全部 PASS 后可置 DONE。` |
| `0.1.15` | `REQ-v0.1.15-003` | `IN_PROGRESS` | 自动评分总分校验与评分项明细展示 | `req-00 4.2` | `是：AI 自动评分区域展示所有维度及其下全部评分项，补充评分项名称、得分、判定标准、判定理由、命中证据与置信度，保持历史无明细记录可兼容展示。` | `是：LLM 自动评分结果入库前按评分项得分重算维度分与总分，保持现有字段结构兼容并不影响规则降级评分链路。` | `1) 模型返回评分项明细时，落库总分等于所有 item_score 之和。2) 工作台自动评分区块展示所有维度和全部评分项，不再仅显示维度摘要。3) 历史无评分项明细的记录可正常查看，不报错。4) 回归自动评分触发、手动重评、候选人总分展示与筛选行为不受影响。` | `TODO` | `自动评分结果总分与评分项累加口径一致，工作台可完整复核每个评分项得分来源，验证计划全部 PASS 后可置 DONE。` |
| `0.1.15` | `REQ-v0.1.15-004` | `DONE` | 上传评分表解析补全维度下评分项 | `req-00 4.2` | `N/A` | `是：补充分段式评分表解析规则，识别维度标题行、评分项行和续行标准；在输出的 dimensions / score_items 中保留正确的维度与评分项归属，并兼容现有平铺表头解析。` | `1) 上传“维度标题行 + 评分项行 + 续行标准”格式评分表后，预览结果中的每个维度都包含对应评分项与评分标准。2) 构建岗位评分项快照时，dimension/point/criterion/score 关系正确。3) 回归已有平铺评分表上传与预览行为不受影响。` | `PASS` | `上传评分表后，分段式评分表可被正确解析为维度下包含评分项的结构，且现有平铺评分表解析能力保持可用，验证计划全部 PASS 后可置 DONE。` |
| `0.1.15` | `REQ-v0.1.15-005` | `DONE` | 治理补充执行前通读与 project-docs 同步规则 | `req-00 4.2` | `N/A` | `N/A` | `1) gov-03-agent-collaboration.md 明确要求每次执行前先读取 docs/docs-index.md 并完整重读 docs/00-governance/。2) gov-03-agent-collaboration.md 明确要求代码或文件变更需同步更新对应 project-docs 描述。3) 交付要求新增 project-docs 是否同步更新的说明项。4) req-00 与 roadmap-00 使用同一 requirement_id 形成闭环。` | `PASS` | `协作治理文档已落地执行前重读与 project-docs 同步规则，且 requirement/roadmap 文档完成闭环记录后可置 DONE。` |

### 平移拆分模板（需求进入 roadmap-00 时填写）

| target_version | requirement_id | task_status | item | draft_ref | frontend_scope | backend_scope | verification_plan | verification_result | done_definition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `0.1.x` | `REQ-v0.1.x-00x` | `PENDING` | 示例：支持候选人筛选 | `req-00 4.x` | `是：筛选输入框、列表联动、空态文案` | `是：新增筛选参数、查询条件拼接` | `1) 接口按姓名、岗位筛选返回正确。2) 页面筛选后列表与计数正确。3) 回归上传与详情查看不受影响。` | `TODO` | `前后端改动点完成，验证项全部 PASS 后可置 DONE。` |

### 执行说明（当前目标版本）

- 当前目标版本已切换到 `0.1.15`。
- `REQ-v0.1.15-001` 已由 `req-00` 确认为 `CONFIRMED` 并平移至当前版本执行清单，状态 `IN_PROGRESS`。
- 开发进展（2026-03-24）：已启动操作记录页面前端开发，新增独立页面骨架、导航入口、筛选区、列表区与详情只读视图；当前页面优先读取后端操作日志接口，后端异常时回退到前端演示数据与岗位页本地日志聚合展示。
- 开发进展（2026-03-24）：前端已补充操作人筛选、导出按钮、关键字段高亮与同对象上一条记录的比对视图；后端已新增统一日志记录表、列表/详情/导出接口，并接入登录、用户权限、岗位配置、候选人维护与流程流转等关键链路留痕。
- `REQ-v0.1.15-002` 已由 `req-00` 确认为 `CONFIRMED` 并平移至当前版本执行清单，状态 `IN_PROGRESS`。
- 开发进展（2026-03-25）：已启动工作台记住上次查看简历的前端开发，采用本地持久化保存当前选中的候选人记录，并在返回工作台后优先恢复该候选人及其详情区。
- `REQ-v0.1.15-003` 已由 `req-00` 确认为 `CONFIRMED` 并平移至当前版本执行清单，状态 `IN_PROGRESS`。
- 开发进展（2026-03-25）：已启动自动评分总分按评分项累加校验与工作台评分项明细完整展示开发，范围涵盖 LLM 结果归一化与工作台 AI 评分区渲染。
- `REQ-v0.1.15-004` 已由 `req-00` 确认为 `CONFIRMED` 并平移至当前版本执行清单，状态 `DONE`。
- 开发进展（2026-03-25）：已启动上传评分表分段结构解析修复，面向“维度标题行 + 评分项行 + 续行标准”的 Excel 评分表补齐维度与评分项归属。
- 验证结果（2026-03-26）：功能验证完成，评分表上传预览与岗位评分项快照均可正确保留维度下评分项归属，`REQ-v0.1.15-004` 已置为 `DONE/PASS`。
- 发布归档（2026-03-26）：用户确认 `REQ-v0.1.15-004` 已发布，已同步归档到 `roadmap-01` 并完成 `req-01/req-02` 回填。
- `REQ-v0.1.15-005` 已由 `req-00` 确认为 `CONFIRMED` 并平移至当前版本执行清单，状态 `DONE`。
- 开发进展（2026-03-26）：已完成协作治理补充，强制要求每次执行前完整重读 `docs/00-governance/`，并将代码或文件变更时同步更新对应 `project-docs` 描述写入治理规则。
- 验证结果（2026-03-26）：治理文档、`req-00` 与 `roadmap-00` 已完成对齐记录，`REQ-v0.1.15-005` 置为 `DONE/PASS`。
- 发布归档（2026-03-26）：用户确认 `REQ-v0.1.15-005` 已发布，已同步归档到 `roadmap-01` 并完成 `req-01/req-02` 回填。
- `v0.1.14` 已完成发布归档，相关 requirement 已转入 `roadmap-01-task-history.md`。

## 4. 版本执行规则（MUST）

1. 进入执行的需求必须先平移到本文档。
2. 平移后初始状态为 `PENDING`。
3. 执行状态只在本文档维护。
4. 平移时必须完成需求拆分：明确 `frontend_scope` 与 `backend_scope`，并写清功能点。
5. 每条需求必须维护 `verification_plan` 与 `verification_result`，未完成验证不得置 `DONE`。
6. `DONE` 在发布时归档到 `roadmap-01`。
